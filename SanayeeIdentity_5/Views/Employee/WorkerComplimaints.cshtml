@model IEnumerable<SanayeeIdentity_5.Data.Complaint>

@{
    ViewBag.Title = "View";
}

<h2>View</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
>
        <th>
            @Html.DisplayNameFor(model => model.datetime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Userid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MessageHead)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Text)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>

        <td>
            @Html.DisplayFor(modelItem => item.datetime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Userid)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MessageHead)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Text)
        </td>
        <td>

            @Html.ActionLink("Delete", "DeleteComplimant", new { Compid = item.ComplaintId, WorkerId = Request.QueryString["WorkerId"] });
            
        </td>
    </tr>
}

</table>
